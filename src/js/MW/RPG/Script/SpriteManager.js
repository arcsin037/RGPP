(function(global) {
	/* global RGPP */
	"use strict";

	var objName = "SpriteManager";
	var constructor = function(spec) {
		var that = {};
		
		
		
		return that;
	};

	RGPP.MW.exportsAsSingleton({
		name: objName,
		constructorFunc: constructor,
		module: module
	});

})((this || 0).self || global);
